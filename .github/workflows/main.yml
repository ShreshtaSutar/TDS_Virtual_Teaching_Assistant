name: Deploy to Render
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy hook
        run: curl <https://tds-project-1-qt1e.onrender.com>
